apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: movie-api-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: staging-movie-api-service
                port:
                  number: 80
